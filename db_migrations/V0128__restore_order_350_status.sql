-- Возврат заказа 350 (PC00028) в исходное состояние: снова в работе,
-- сборка #133 (wip 43) привязана к нему, с витрины снята. Данные клиента
-- НЕ трогаем — пустые, как было. Клиент перезакажет через сайт.
UPDATE t_p72635010_quantum_fusion_resea.orders SET status = 'waiting_assembly', updated_at = NOW() WHERE id = 350;