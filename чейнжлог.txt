# CHANGELOG — история изменений проекта
# Формат: v{номер} | {дата} | {краткое название}

---

## v1.1 | 2026-06-15 | Аудит и исправление структуры

### Изменения
- [структура.txt] Исправлен статус заказов: было ошибочно 'active', стало 'new'
  (реальный статус по умолчанию при создании заказа)
- [структура.txt] Переписан раздел про WIP — чётко разделены:
  ТАБЛИЦА wip_builds (живая, нужная, не удалять) и
  ПАПКА /backend/wip-builds/ (мёртвая, нет URL, код не выполняется)
- [структура.txt] Запись про notifications 401 перенесена в открытые задачи

### Открытые задачи (требуют решения)
- [WIP] Frontend вызывает несуществующий URL (cb6e9d4e-...) для операций:
  создание/обновление сборки, смена этапа, смена статуса компонента.
  Эти функции сейчас НЕ РАБОТАЮТ. Нужно восстановить функцию или перенести логику.
- [notifications] HTTP 401 каждые 30 сек — polling во фронтенде без авторизации.

---

## v1.0 | 2026-06-15 | Базовая очистка и фикс отмены WIP-заказов

### Изменения
- [warehouse] Добавлена и затем удалена кнопка "Очистить заказы" в шапке склада
  (не удалось решить из-за FK constraint — orders ссылается на user_id и warehouse_movements)
- [builds] Добавлен action `cancel_order` (POST):
  - POSITIVE резервы → возвращает qty в наличие, снимает qty_reserved
  - NEGATIVE резервы → убирает qty_negative (незакупленное железо)
  - Заказ → статус 'archived' (вместо 'cancelled')
  - pc_build → статус 'archive'
  - wip_build → удаляется
- [AdminWipTab] Обновлён текст в модалке подтверждения отмены
- [структура.txt] Создан файл описания функций
- [чейнжлог.txt] Создан этот файл

### Технические детали
- Логика cancel_order перенесена из backend/wip-builds/ (не деплоится) в backend/builds/index.py
- wip-builds функция не деплоится: runtime определяется как nodejs22 вместо python311