# Складская логика — чеклист багов и проверок

Файл для отлова и фиксации багов по механике «Минус-резерв + Автозакупка».
Статусы: ⬜ не проверено · 🟡 проверяется · ✅ ок · 🔴 баг

## Этап 1 — Ядро резервов

### Создание заказа (handle_reserve_and_purchase)
- ✅ 10/0/5 → POSITIVE 5, закупки нет (selftest)
- ✅ 0/0/5 → NEGATIVE -5, basket +5 (selftest)
- ✅ 3/0/5 → POSITIVE 3 + NEGATIVE -2, basket +2 (selftest)
- ✅ 0/0/0 → ничего не делать (selftest)
- ⬜ позиция без product_id (пользовательское железо) → пропущена + пометка (код есть, тест нужен)
- ⬜ позиция с qty ≤ 0 (возврат) → минус-резерв НЕ применён (код есть, тест нужен)
- ⬜ POSITIVE раскладывается по партиям FIFO, не в одну партию (код есть, multi-supply тест нужен)
- ⬜ движения warehouse_movements пишутся на каждую операцию (код есть, проверить SQL-ом)

> Самотест: GET ?action=selftest на функции reserves — прогоняет 4 сценария с откатом.

### Конкурентность
- ⬜ два заказа на 1 шт одновременно → второй уходит в NEGATIVE (FOR UPDATE работает)
- ⬜ нет ухода физического остатка в минус (qty партии не становится < 0)

### Отмена заказа
- ⬜ POSITIVE → вернулся в наличие (qty += r, qty_reserved -= r)
- ⬜ NEGATIVE при backorder NEW → qty_negative и backorders.qty уменьшены
- ⬜ NEGATIVE при backorder ORDERED → НЕ тронут

### Пересчёт (RecalcOrderReserves) ⚠️ ОСОБОЕ ВНИМАНИЕ
- ⬜ изменение кол-ва вверх → добавочный резерв корректен
- ⬜ изменение кол-ва вниз → лишний резерв снят
- ⬜ удаление позиции → резерв позиции снят полностью
- ⬜ добавление позиции → новый резерв создан
- ⬜ двойной вызов пересчёта подряд не дублирует резервы (идемпотентность)
- ⬜ пересчёт не затирает уже заказанные у поставщика backorder-ы

### Целостность данных (инварианты, проверять SQL-ом)
- ⬜ Σqty_reserved по группе == Σ POSITIVE резервов активных заказов
- ⬜ Σqty_negative по группе == Σ backorders.qty (NEW+ORDERED) по группе
- ⬜ нет партий с qty < 0
- ⬜ нет «осиротевших» резервов (заказ удалён, резерв остался)

## Известные риски текущей системы (до рефакторинга)
- 🔴 backorders в БД заполнены, но бэкенд их не использует (потребность считалась через wip need_order) — рассинхрон
- 🔴 qty_negative разбросаны по партиям непоследовательно (group 13: партия qty=0 имеет reserved=3, negative=2)
- 🔴 reserve/unreserve без FOR UPDATE — гонки
- 🔴 корзина закупки (статусы) хранилась в localStorage браузера — не источник истины

## Лог-наблюдения (заполняется автоматически в процессе работы)
<!-- сюда дописываются выводы из stock_log при анализе -->