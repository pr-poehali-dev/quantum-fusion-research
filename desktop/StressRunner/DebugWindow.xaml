<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    x:Class="StressRunner.DebugWindow"
    Title="Датчики — отладка" Height="600" Width="720"
    WindowStartupLocation="CenterOwner"
    Background="{DynamicResource Bg}"
    FontFamily="Segoe UI">

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Отладка датчиков HWiNFO" FontSize="17" FontWeight="SemiBold" Margin="0,0,0,8"/>
        <Border Grid.Row="1" Background="{DynamicResource Muted}" BorderBrush="{DynamicResource Border}" BorderThickness="1"
                CornerRadius="8" Padding="11,9" Margin="0,0,0,12">
            <TextBlock x:Name="StatusText" Text="—" FontSize="12.5" TextWrapping="Wrap" Foreground="{DynamicResource Fg}"/>
        </Border>

        <Border Grid.Row="2" Background="{DynamicResource Card}" BorderBrush="{DynamicResource Border}" BorderThickness="1" CornerRadius="10">
            <DataGrid x:Name="Grid" AutoGenerateColumns="False" IsReadOnly="True"
                      Background="Transparent" Foreground="{DynamicResource Fg}"
                      GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                      RowBackground="Transparent" BorderThickness="0" Margin="4"
                      FontSize="12">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Тип" Binding="{Binding Type}" Width="70"/>
                    <DataGridTextColumn Header="Метка" Binding="{Binding Label}" Width="*"/>
                    <DataGridTextColumn Header="Значение" Binding="{Binding Value}" Width="90"/>
                    <DataGridTextColumn Header="Min" Binding="{Binding Min}" Width="80"/>
                    <DataGridTextColumn Header="Max" Binding="{Binding Max}" Width="80"/>
                    <DataGridTextColumn Header="Ед." Binding="{Binding Unit}" Width="60"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,12,0,0">
            <Button x:Name="RefreshBtn" Content="↻ Обновить" Style="{StaticResource PrimaryButton}"/>
            <Button x:Name="CopyBtn" Content="Скопировать список" Margin="10,0,0,0"/>
        </StackPanel>
    </Grid>
</Window>
